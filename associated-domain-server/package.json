{
  "name": "domain-association-server",
  "version": "1.0.0",
  "description": "Simple server to serve apple-app-site-association for ngrok tunneling during iOS development",
  "main": "server.js",
  "scripts": {
    "start": "dotenvx run --env-file=../.env.dev -- node server.js",
    "dev": "dotenvx run --env-file=../.env.dev -- node server.js"
  },
  "dependencies": {
    "@dotenvx/dotenvx": "^1.51.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
